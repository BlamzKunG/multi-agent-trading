# Multi-Agent Trading Orchestrator

ระบบบอทเทรดทองคำ (XAUUSD) และสินทรัพย์ดิจิทัลแบบทำงานตามรอบเวลา (Stateless Loop) ขับเคลื่อนด้วยระบบสถาปัตยกรรมตัวแทนหลายตัว (Multi-Agent Architecture) ประมวลผลผ่านโมเดลภาษาขนาดใหญ่ (LLMs) และเชื่อมต่อผ่านระบบ MetaTrader 5

---

## ส่วนประกอบของระบบ (System Architecture)

ระบบถูกออกแบบมาแยกส่วนเพื่อให้ง่ายต่อการขยายและทดสอบ ประกอบด้วยไฟล์หลักดังนี้:

1. **bot_orchestrator.py**: ตัวควบคุมลูปการทำงานหลักในโหมดจำลอง (Simulation Mode)
2. **bot_orchestrator_mt5.py**: ตัวควบคุมลูปการทำงานหลักร่วมกับแพลตฟอร์ม MetaTrader 5 (Live Mode)
3. **gui_dashboard.py**: ระบบหน้าจอควบคุมหลักของแอปพลิเคชัน (Desktop Dashboard GUI) พัฒนาด้วย Tkinter สำหรับกำหนดค่าความเสี่ยง คีย์การใช้งาน และเปิด-ปิดระบบทำงานอัตโนมัติ
4. **trading_agents.py**: คลาสการทำงานร่วมกับ AI Agent ทั้งในส่วนการวิเคราะห์ทางเทคนิค (Analyst Agent) และการบริหารพอร์ตโฟลิโอและความเสี่ยง (Manager Agent)
5. **mt5_integration.py**: โมดูลสำหรับเชื่อมต่อ สั่งการเปิด-ปิดออเดอร์ ตลอดจนการทำระบบแก้ไขการตั้ง Stop Loss และ Take Profit ให้สอดคล้องกับ Stops Level ของโบรกเกอร์ (Self-Healing Logic)
6. **exchange_sim.py**: ระบบจำลองสภาวะการซื้อขายในตลาดจำลอง สำหรับทดสอบตรรกะและการคำนวณ Margin
7. **data_feed.py**: ตัวดึงข้อมูลราคาสินทรัพย์จริงผ่าน API สาธารณะ

---

## คุณสมบัติหลัก (Core Features)

- **ระบบวิเคราะห์ Multi-Agent M5 Price Action Scalper (สไนเปอร์กราฟเปล่า):**
  * **Trend & Structure Agent (LLM):** วิเคราะห์เทรนและโครงสร้างราคาบนกรอบ M15/H1 ชี้เป้าแนวรับ-ต้านหลักสรุปฝั่งที่ได้เปรียบ (`BUY ONLY`, `SELL ONLY`, `HOLD`)
  * **Price Action Analyst Agent (LLM):** ประเมินพฤติกรรมกราฟเปล่าและรูปแบบแท่งเทียน M5 (ตัวเนื้อเทียน, ขนาดไส้บน/ล่าง, การเบรคตัวหรือถูกปฏิเสธราคาย้อนหลัง 10 แท่ง) เพื่อหาจุดเข้าและระยะ SL
  * **Volatility & Spread Guard (Python):** กรองข่าวสาร/สเปรดโบรกเกอร์ที่กว้างเกินค่าธรรมเนียมยอมรับ และปริมาณความแกว่งตลาดขั้นต่ำ (ATR Filter)
  * **Risk Manager (Python):** คำนวณขนาดล็อตคงที่จำกัดความเสี่ยงไม่เกิน 1% ของพอร์ต และตั้งระยะ SL แรกเริ่มตามความผันผวน ATR
  * **Scalp Master / CIO Agent (LLM):** รวมมติวิเคราะห์และตัดสินใจร่วมเอกฉันท์เพื่อยิงออเดอร์ในรูปแบบ JSON หากชิ้นส่วนการประเมินชิ้นใดเกิดขัดแย้งหรือขาดหาย จะตัดสินเป็น `HOLD` ทันทีเพื่อรักษาทุนพอร์ต
- **ระบบเก็บกำไรและ Stop Loss สองระดับ (Dynamic Trailing Stop):**
  * **Simple Trailing Stop (Python-based):** ตรวจจับจังหวะราคาขยับไปในทิศทางได้เปรียบ และเลื่อนจุดตัดขาดทุน (SL) มาบังหน้าทุนแบบไร้ความหน่วง โดยประมวลผลฝั่งโปรแกรมบอททันที ไม่ต้องเรียกผ่าน API เพิ่มเติมเพื่อประหยัดเครดิต
  * **Self-Healing Logic:** ดักจับค่า Stops Level โบรกเกอร์ เพื่อคำนวณและปรับระยะ Stop Loss / Take Profit ให้เป็นไปตามกฎของ MT5 อัตโนมัติ ป้องกันการส่งคำสั่งผิดพลาด (Invalid Stops)
- **ระบบทำงานเสถียรทนทาน (Robust Fail-safe & Timeout Optimization):**
  * **Auto-Fallback Engine:** ระบบสลับโมเดล AI ลำดับถัดไปแบบอัตโนมัติหากโมเดลหลักขัดข้อง (เช่น `claude-sonnet-5` -> `claude-sonnet-4-6` -> `claude-haiku-4-5-20251001`)
  * **90-Second Timeout:** ขยายเวลารอรับสัญญาณ API เพื่อรองรับโมเดลคิดลึก (Thinking Models) ที่ใช้เวลาจัดเรียงความคิดยาวนานกว่ามาตรฐานทั่วไป
- **ระบบแจ้งเตือนเหตุการณ์ผ่าน Discord Webhook:** ส่งรายงานมติและข้อคิดเห็นของประธานกองทุน รายละเอียดออเดอร์เปิด/ปิด และการขยับจุด SL หน้าไม้ให้รับรู้ทันใจผ่าน Discord

---

## การเตรียมการติดตั้ง (Installation)

### 1. โคลนคลังเก็บรหัสต้นฉบับ (Repository)
```bash
git clone https://github.com/BlamzKunG/multi-agent-trading.git
cd multi-agent-trading
```

### 2. ติดตั้งไลบรารีที่จำเป็น
```bash
pip install pandas requests
```

หากต้องการใช้งานร่วมกับพอร์ตจริงผ่าน MetaTrader 5 (สามารถใช้งานได้บนระบบปฏิบัติการ Windows เท่านั้น):
```bash
pip install MetaTrader5
```

---

## การกำหนดค่าและการใช้งาน (Configuration and Usage)

### การตั้งค่าคีย์การเข้าถึง API
กรุณากำหนดคีย์เข้าใช้งาน MaxPlus API ในตัวแปรสภาพแวดล้อม (Environment Variables) ของระบบปฏิบัติการก่อนเริ่มต้นรันบอท:

- **Windows (Command Prompt):**
  ```cmd
  set MAXPLUS_API_KEY="คีย์ของคุณ"
  ```
- **macOS / Linux:**
  ```bash
  export MAXPLUS_API_KEY="คีย์ของคุณ"
  ```

### การเปิดใช้งานระบบควบคุมผ่านหน้าต่างแสดงผล (Desktop GUI)
รันไฟล์แสดงผลหลักเพื่อควบคุม กำหนดสิทธิ์บัญชี และตั้งเวลาทำงานอัตโนมัติ:
```bash
python gui_dashboard.py
```
เมื่อหน้าต่างโปรแกรมเปิดขึ้นมา คุณสามารถกำหนดเงื่อนไขดังต่อไปนี้ได้:
- สลับโหมดการทำงาน (Simulation / MT5 Live)
- กรอกรหัสการล็อกอิน MT5, รหัสผ่าน และโบรกเกอร์เซิร์ฟเวอร์
- กำหนดสัดส่วนจำกัดล็อตสูงสุด (Max Allowed Lot size) และรอบเวลาเป็นนาที (Interval Cycle)
- เปิดระบบความคุมอัตโนมัติ (Auto-Pilot)
- สั่งปิดออเดอร์ หรือกดสั่งการวิเคราะห์ทันทีแบบ Manual

### การทดสอบระบบซื้อขายจำลองผ่าน CLI
สำหรับทดสอบระบบในสภาพแวดล้อมจำลอง (Sandbox):
```bash
python bot_orchestrator.py
```

---

## สัญญาอนุญาต (License)

ซอฟต์แวร์นี้เผยแพร่ภายใต้สัญญาอนุญาต MIT (MIT License)

ลิขสิทธิ์ (c) 2026 BlamzKunG

อนุญาตให้บุคคลใดๆ ที่ได้รับสำเนาของซอฟต์แวร์นี้และไฟล์เอกสารที่เกี่ยวข้อง สามารถใช้งานซอฟต์แวร์ได้โดยไม่มีข้อจำกัด รวมถึงสิทธิ์ในการใช้งาน ทำซ้ำ ปรับปรุง รวม เผยแพร่ แจกจ่าย ให้สิทธิ์ช่วง หรือขายซอฟต์แวร์ ภายใต้เงื่อนไขที่จะต้องแสดงประกาศลิขสิทธิ์นี้และประกาศข้อกำหนดความยินยอมด้านบนไว้ในสำเนาหรือส่วนสำคัญทั้งหมดของซอฟต์แวร์

ซอฟต์แวร์นี้จัดหาไว้ "ตามที่เป็น" โดยไม่มีการรับประกันใดๆ ไม่ว่าจะโดยชัดแจ้งหรือโดยนัย